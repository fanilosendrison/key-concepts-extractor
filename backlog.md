# Backlog

- [ ] [NOTABLE] RC on-disk schema breaking change — `~/.kce/runs/*/*-relevance.json` pré-L20 (si existants) sont structurellement incompatibles avec le nouveau `RelevanceRemoval`/`RelevanceRetention` (term/justification_* vs target/reason/defense). Pas de bug actif (RC était fail-closed depuis L19, donc pas de vraies runs persistées), mais tout consommateur qui lit d'anciens JSONs échouera. Aucune migration spec'd en §3.7. Décider : migration layer ou versioning. (date: 2026-04-16, source: senior-review post-RC-spec-align)
- [ ] [NOTABLE] src/pipeline.ts:166 — `fuseInterAngle` n'a pas de signal threadé. Si `EmbeddingAdapter.embed` supporte `AbortSignal`, ajouter `signal: deps.signal` au call. Sinon étendre le port. Dernier await long-running sans cancel responsiveness. (date: 2026-04-16, source: senior-review post-L19)
- [ ] [NOTABLE] tests — aucune couverture du path cancellation : `composeSignal` (compose external + timeout), `runWithRetry` avec abort pendant backoff (sleep doit rejeter, pas de retry supplémentaire), abort pendant fetch. Ajouter tests unitaires infra/provider-shared.test.ts. (date: 2026-04-16, source: senior-review post-backlog-pass)
- [ ] [MINOR] tests/run-manager.test.ts — branche `persistDiagnostics` sans `persistInterAngle` préalable (warn path sur merged.json manquant) non testée. Ajouter T-RM-XX qui appelle persistDiagnostics seul, vérifie que diagnostics.json existe et merged.json absent. (date: 2026-04-16, source: senior-review post-backlog-pass)
- [ ] [MINOR] src/domain/coverage-verifier.ts — `\b` ASCII-only, pas de flag `u`. Termes accentués ("café", "naïve") ou caractères composés NFD peuvent faux-négativer. Passer à `(?<![\p{L}\p{N}])TERM(?![\p{L}\p{N}])` avec flag `u`. Spec §6 admet cette limitation v1, donc low priority. (date: 2026-04-16, source: senior-review post-backlog-pass)
- [ ] [NIT] tests/helpers/mock-embedding.ts — si un terme apparaît anchor d'une paire puis partner d'une autre, l'invariant cosine==sim casse silencieusement. Ajouter assertion `if (placed.has(a)) throw new Error("mock-embedding: term anchor in multiple pairs not supported")` ou supporter le cas. Tests actuels pas impactés. (date: 2026-04-16, source: senior-review post-backlog-pass)
- [ ] [NIT] src/domain/quality-controller.ts callLLMRound — `control: "quality"` en dur dans l'emit. Quand RC utilisera le même helper (via REFACTOR L17), passer `control: "quality" | "relevance"` en paramètre. (date: 2026-04-16, source: senior-review post-backlog-pass)

- [ ] [NOTABLE] specs/NIB-S-KCE.md §3.10 + §3.15 — divergence avec impl `RunConfig` actuelle. Spec §3.10 dit `Record<ProviderId, string>` (short ids dans ce codebase = claude/gpt/gemini) mais l'exemple §4.2 utilise les long ids `{anthropic,openai,google}` ; impl suit l'exemple. Spec §3.15 PipelineConfig groupe par provider `{apiKey, model, endpoint}` alors qu'impl utilise un sous-ensemble plat `RunConfig`. Décider : amender les specs ou refactor l'impl. (date: 2026-04-15, source: senior-review post-PipelineConfig)

- [ ] [MINOR] src/cli/index.ts — Gap : le CLI parse des paths de fichiers (`string[]`) mais le pipeline attend `InputFile[]` (avec content chargé). Scaffolder un adapter `loadInputFiles(paths: string[]): Promise<InputFile[]>` côté infra. (date: 2026-04-15, source: senior-review RED scaffold)
- [ ] [NOTABLE] src/domain/types.ts — `ConceptCategory`, `GranularityLevel` et `PipelineEvent.type` typés `string`. Trou type-level : un LLM qui renvoie "méthod" au lieu de "method" passe le type checker mais corrompt le pipeline downstream. Resserrer en unions littérales en consultant NIB-S §3 et NIB-M-EVENT-LOGGER. (date: 2026-04-15, source: senior-review RED scaffold ; upgraded MINOR→NOTABLE le 2026-04-16)
- [ ] [MINOR] tests/run-manager.test.ts:50 — T-RM-04 flaky : run_id à résolution 1s + suffixe nanoid, deux runs créés en <1s peuvent avoir un ordre non déterministe. Ordonner via `created_at` du manifest plutôt que par nom. (date: 2026-04-15, source: senior-review RED scaffold)
- [ ] [MINOR] tests/helpers/control-responses.ts — Schémas R1/R2/R3 inférés de NIB-M-QC/RC §4 (pas de NIB-M-LLM-PAYLOADS lu en détail). À valider/ajuster en GREEN contre les schémas exacts des prompts définis par NIB-M-LLM-PAYLOADS. (date: 2026-04-15, source: implémentation treat-backlog)
- [ ] [MINOR] tests/helpers/mock-provider.ts:4 — Export `MockCallRecord` non importé ailleurs. À garder pour introspection future ou supprimer si non utilisé en GREEN. (date: 2026-04-15, source: senior-review post-treat-backlog)
- [ ] [MINOR] src/web/server.ts:6 — Renommer `WebServerHandle` en `ListeningServer` ou fusionner avec `WebServer` pour cohérence avec convention Node. (date: 2026-04-15, source: senior-review post-treat-backlog)
- [ ] [REFACTOR] src/domain/quality-controller.ts + relevance-controller.ts — squelette 3-rounds dupliqué. Extraire `runThreeRoundControl<R1,R2,R3,Output>()` générique paramétré par schémas et fonction d'agrégation. (date: 2026-04-15, source: dedup-codebase GREEN)
- [ ] [SPEC-DESIGN] src/domain/types.ts withTerm — héritage intégral de `angle_provenance`, `angles_count`, `justifications` lors du split est **spec-prescribed** (NIB-M-QUALITY-CONTROLLER §4.4 ligne 216 : "Other fields [...] `angle_provenance` are inherited as-is" + ligne 190-192 : "QC may re-flag on subsequent pass if inherited category/granularity is incorrect"). Le compromis est assumé : sur-attribution temporaire acceptée, re-correction possible au pass suivant. Code conforme. Si remise en cause souhaitée → discussion spec séparée (impacte NIB-M-QC + DC-* + prompts LLM). (date: 2026-04-15, source: senior-review post-MAJOR-fixes ; downgraded MAJOR→SPEC-DESIGN le 2026-04-16 après vérif spec §4.4)
- [ ] [NOTABLE] tests/quality-controller.test.ts T-QC-02:55 — `.sort()` sur terms masque l'ordre réel du split. Vérifier si NIB-M-QUALITY-CONTROLLER §4.4 prescrit que l'ordre de `suggested_split` est préservé ; si oui, retirer le sort et asserter l'ordre strict. (date: 2026-04-15, source: senior-review post-MAJOR-fixes)
- [ ] [NIT] tests/helpers/control-responses.ts — helper force `suggested_split: null` quand caller passe `undefined`. Distinction absent-vs-null peut masquer des divergences entre schéma mock et schéma LLM réel. Faible impact. (date: 2026-04-15, source: senior-review post-MAJOR-fixes)
- [ ] [NIT] src/domain/quality-controller.ts — `QualityControlSchemaError` exporté mais utilisé uniquement en interne. Retirer `export` ou garder et documenter comme classe d'erreur publique. (date: 2026-04-15, source: dedup-codebase post-MAJOR-fixes)
- [ ] [NIT] src/web/server.ts WebSocket — si `ws.send` échoue après socket half-closed (TCP reset sans close frame), onClose peut ne jamais se déclencher → listener leaké dans `subscribersByRunDir` jusqu'à fin de process. Dans le catch autour de `ws.send`, appeler explicitement unsub + cleanup. (date: 2026-04-15, source: senior-review post-MAJOR-fixes)
- [ ] [NIT] src/web/server.ts — `WebServerHandle` : nommage "handle" faible côté consommateurs. Renommer `ListeningServer` ou fusionner avec `WebServer`. Déjà noté ligne 9, confirmé. (date: 2026-04-15, source: senior-review post-MAJOR-fixes)
- [ ] [MINOR] scripts/spec-drift.ts — aucun test pour le script. Une regression dans le matching de prefixes (Spec_B###_) ou la classification des diagnostics passerait silencieusement. Ajouter tests/scripts/spec-drift.test.ts avec 3 cas : bloc OK, bloc DRIFT (champ ajoute cote spec), bloc MISSING. (date: 2026-04-16, source: senior-review post-A+D)
- [ ] [NIT] scripts/spec-drift.ts — `padStart(3, "0")` hardcode pour le prefixe `B###_`. Casserait le tri lexico si on depasse 999 blocs (aucun risque court terme : ~50 blocs actuellement). Augmenter a `padStart(4, "0")` si necessaire. (date: 2026-04-16, source: senior-review post-A+D)
