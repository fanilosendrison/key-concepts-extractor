# Backlog

- [ ] [notable][blocked:prod-data] `src/infra/provider-shared.ts:10` — la valeur 600s n'est pas backée par des données empiriques (p95 latency). Instrumenter la latence des adapters LLM et ajuster data-driven. **Bloqué** : nécessite des runs LLM réels pour mesurer p95 ; `latencyMs` est déjà retourné par runWithRetry, aggregation/rollup à implémenter post-lancement (ne peut pas être fixé hors-prod). (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [minor] `src/cli/format-event.ts:59-63` + `:46-48` — `ExtractionCompletePayload` et `RunErrorPayload` sous-déclarent les clés réellement émises (concepts_dropped, fatal) ; zod strip les extras silencieusement. Ajouter les clés optionnelles au schema ou commenter explicitement la tolérance pour bloquer une future dérive vers `.strict()`. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [minor] `src/cli/format-event.ts:21` — `timestamp: z.string()` accepte "" ou "garbage" et produit un slice(11,23) vide/corrompu dans formatEvent. Tighten vers `z.iso.datetime()` pour bloquer en replay les timestamps non-ISO. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [minor] `src/infra/logger.ts:12` — destination bindée au module load, non injectable. Pas de signal aujourd'hui (LOG_LEVEL=silent en tests) mais foreclose les tests qui captureraient la sortie pino. Exporter un `createLogger(destination?)` si besoin. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [minor] `src/cli/commands/show.ts:25,33` — `raw.split("\n")` ne gère pas CRLF et le preview d'erreur inclut le `\r` (corrompt le terminal). Remplacer par `raw.split(/\r?\n/)` + sanitize le preview. Pre-existing + Chrome-only → faible priorité. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [minor] `tests/openai-embedding-adapter.test.ts` — pas de test "batch N/M échoue fatal après que les précédents ont réussi". Sémantique actuelle = all-or-nothing (stack unwind perd les vecteurs partiels), raisonnable mais non verrouillée par test. Fix : ajouter T-EMB-09 qui fait échouer le 2e batch (mock bifurqué sur callIndex) et vérifie que `embed()` rejette sans retourner de vecteurs partiels. (date: 2026-04-17, source: senior-review post-batching-fix)
- [ ] [minor] `tests/provider-shared.test.ts:15-20` — T-PS-02 ne teste pas le comportement effectif d'un `timeoutMs` court valide (seul T-PS-10 teste les invalides). Ajouter un test qui passe un `timeoutMs` court (ex. 10ms), attend, et vérifie que le signal composite est aborted avec `name === "TimeoutError"`. (date: 2026-04-17, source: senior-review post-options-refactor)
- [ ] [minor] `src/infra/{anthropic,openai,google,openai-embedding}-adapter.ts` — constante `DEFAULT_ENDPOINT` et pattern `const endpoint = config.endpoint ?? DEFAULT_ENDPOINT` dupliqués dans 4 fichiers. Centraliser en `DEFAULT_ENDPOINTS: Record<ProviderLongId, string>` dans `provider-shared.ts` + helper `resolveEndpoint`. Gain : ~4 lignes, typage plus fort. (date: 2026-04-17, source: dedup-codebase post-batching-fix)
- [ ] [minor] `src/infra/openai-adapter.ts` + `src/infra/google-adapter.ts` — validation `finish_reason` + extraction content dupliquée (truncation → TransientLLMError, safety/content_filter → FatalLLMError, puis extract content). Envisager un helper `extractLLMContent<T>(data, extractors)` dans `provider-shared.ts` avec extracteurs par provider. Non-trivial : les shapes varient (choices[0].finish_reason vs candidates[0].finishReason vs content[] blocks), la factorisation doit rester plus lisible que la duplication. (date: 2026-04-17, source: dedup-codebase post-batching-fix)
- [ ] [minor] `src/infra/provider-shared.ts:57-60` vs `:93-96` — gardes numériques dupliqués (`!Number.isFinite / ≤0`) entre `composeSignal` et `runWithRetry`, avec sémantique subtilement différente (composeSignal refuse 0, runWithRetry accepte 0). Extraire `assertPositiveIntegerMs(name, value)` et `assertNonNegativeFiniteMs(name, value)` dans un helper. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [minor] `src/infra/provider-shared.ts:81` — un timeout interne (`AbortSignal.timeout` fire) avec signal externe non aborted retombe en `TransientLLMError` et retry. Aligné avec NIB-M-PROVIDER-ADAPTERS mais aggrave le problème de budget global. À clarifier dans la spec ou documenter l'interaction (date: 2026-04-17, source: senior-review post-timeout-bump)
