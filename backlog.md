# Backlog

- [ ] [major] `src/infra/provider-shared.ts` — pas de `MAX_TOTAL_DURATION_MS` global ; pire cas par call = 4 × TIMEOUT_MS + backoff ≈ 41 min avec TIMEOUT_MS=600_000. Lock mono-run (Interface Spec §7.2) peut être bloqué plusieurs heures. Ajouter un deadline global ou documenter l'invariant (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [notable] `src/infra/provider-shared.ts` — `TIMEOUT_MS` partagé entre tous les adapters via `composeSignal`, y compris `openai-embedding-adapter.ts` (appels courts 1-3s). Bump à 600s masque un embedding qui pendrait 10 min. Introduire `TIMEOUT_MS_EMBEDDING` ou override per-caller (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [notable] `src/infra/provider-shared.ts:10` — la valeur 600s n'est pas backée par des données empiriques (p95 latency). Instrumenter la latence des adapters LLM et ajuster data-driven (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [minor] `src/infra/provider-shared.ts:81` — un timeout interne (`AbortSignal.timeout` fire) avec signal externe non aborted retombe en `TransientLLMError` et retry. Aligné avec NIB-M-PROVIDER-ADAPTERS mais aggrave le problème de budget global. À clarifier dans la spec ou documenter l'interaction (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [notable] `src/cli/commands/run.ts:78` — double output stdout sur fin de run : event `run_complete` (via subscriber) + ligne humaine `Run ... — {status}`. Décider : supprimer la ligne humaine maintenant que l'event arrive de façon fiable, ou documenter le choix du double output (date: 2026-04-17, source: senior-review post-timeout-bump)
