# Backlog

- [ ] [SPEC-DESIGN] src/domain/types.ts withTerm — héritage intégral de `angle_provenance`, `angles_count`, `justifications` lors du split est **spec-prescribed** (NIB-M-QUALITY-CONTROLLER §4.4 ligne 216 : "Other fields [...] `angle_provenance` are inherited as-is" + ligne 190-192 : "QC may re-flag on subsequent pass if inherited category/granularity is incorrect"). Le compromis est assumé : sur-attribution temporaire acceptée, re-correction possible au pass suivant. Code conforme. Si remise en cause souhaitée → discussion spec séparée (impacte NIB-M-QC + DC-* + prompts LLM). (date: 2026-04-15, source: senior-review post-MAJOR-fixes ; downgraded MAJOR→SPEC-DESIGN le 2026-04-16 après vérif spec §4.4)
- [ ] [MINOR] tests/helpers/control-responses.ts:fullR2Review — `claude_error_type` default "abusive_merge" peut mentir dans les scénarios où R1 a flaggé `incorrect_categorization` ou `justification_incoherence`. Si le contrôleur ajoute un cross-check R2↔R1 (prévu par spec Type 3), les tests passeront contre un bug. Fix : dériver le default du caller ou rendre le param obligatoire pour non-abusive_merge. (date: 2026-04-16, source: senior-review post-MINOR-batch)
- [ ] [NIT] src/domain/coverage-verifier.ts — pas de cache RegExp entre appels. `verifyCoverage` compile N × M regex sur gros corpus. Memoize par `trimmed` via Map module-level. Perf marginale aujourd'hui (fixtures petites). (date: 2026-04-16, source: senior-review post-MINOR-batch)
- [ ] [NIT] src/cli/commands/history.ts — N reads séquentiels sur historique de runs (latence ~1ms/run, ~500ms sur 500 runs). Soit `Promise.all` sur la map, soit persister `prompt_preview` dans `manifest.json` à l'init (source of truth, élimine les reads + résout truncation unicode). (date: 2026-04-16, source: senior-review post-MINOR-batch)
- [ ] [NIT] src/cli/commands/history.ts — `.slice(0, 60)` opère sur UTF-16 code units, peut couper un emoji ou un char combinant en plein milieu → U+FFFD au rendu. Pré-existant. Fix propre : `Intl.Segmenter` avec granularity "grapheme". (date: 2026-04-16, source: senior-review post-MINOR-batch)
