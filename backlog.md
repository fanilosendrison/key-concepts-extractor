# Backlog

- [ ] [notable][blocked:prod-data] `src/infra/provider-shared.ts:10` — la valeur 600s n'est pas backée par des données empiriques (p95 latency). Instrumenter la latence des adapters LLM et ajuster data-driven. **Bloqué** : nécessite des runs LLM réels pour mesurer p95 ; `latencyMs` est déjà retourné par runWithRetry, aggregation/rollup à implémenter post-lancement (ne peut pas être fixé hors-prod). (date: 2026-04-17, source: senior-review post-timeout-bump)
- [x] [nit] `src/infra/provider-shared.ts` — convention `name: "fnName: paramName"` des asserters (assertPositiveIntegerMs/assertNonNegativeFiniteMs) non documentée dans la signature. Si un 3e asserter arrive, risque de divergence de la convention. Fix : JSDoc `@param name "functionName: paramName"` OU refactor `(fnName, paramName, value)`. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [x] [nit] `src/cli/commands/show.ts:40` — `line.slice(0, 80)` peut couper une surrogate pair UTF-16 en fin de preview, produisant un caractère de remplacement. Edge case improbable (JSON valide n'a pas de surrogates isolés, et la ligne est déjà malformed par hypothèse). Fix : append `.replace(/[\uD800-\uDBFF]$/, "")` après le scrub. (date: 2026-04-17, source: senior-review post-notable-fixes)

## Test typecheck debt — exposed by `tsconfig.test.json` / `pnpm typecheck:tests`

Contexte : le `tsconfig.json` projet excluait `tests/` du type-checking. L'ajout de `tsconfig.test.json` + script `typecheck:tests` a exposé 8 erreurs pré-existantes dans 5 fichiers de tests. Runtime OK (vitest 168/168), mais les invariants de types dérivent. À reboucher lot par lot ; une fois vide, câbler `typecheck:tests` dans la liste de vérifications obligatoires (CLAUDE.md projet) et en CI quand elle arrive.

- [x] [notable] `tests/event-logger.test.ts:35,44,53` — `"tick"` passé comme PipelineEventType n'est pas dans `PIPELINE_EVENT_TYPES`. Soit le test simule un event synthétique qui devrait être rebaptisé en type publié, soit le test pré-date la fermeture de l'enum. Fix : renommer vers un type valide (ex. `extraction_progress`) ou caster explicitement avec justification. (date: 2026-04-17, source: typecheck:tests)
- [x] [notable] `tests/fusion-intra.test.ts:6` — helper local accepte `string` pour un paramètre typé `ConceptCategory`. Erreur TS2322 masquée. Fix : typer le helper avec `ConceptCategory` et faire traverser les littéraux fermés. (date: 2026-04-17, source: typecheck:tests)
- [x] [notable] `tests/run-manager.test.ts:50` — `RawConcept[]` avec objets partiels (manquent `granularity`, `justification`). Fix : compléter les fixtures ou utiliser la factory `rawConcept()` de `tests/helpers/factories.ts` qui remplit les defaults. (date: 2026-04-17, source: typecheck:tests)
- [x] [notable] `tests/web-server.test.ts:88,91` — `rm.initRun(DEFAULT_RUN_CONFIG)` passe 1 argument ; la signature prod est `initRun(config, source)` depuis run-manager.ts:24. Signature drift masquée par l'absence de typecheck tests — le `source` est `undefined` à runtime, ce qui traverse silencieusement. Fix : passer un `RunSource` approprié (probablement `"cli"` ou `"web"` selon le scénario). (date: 2026-04-17, source: typecheck:tests)
- [x] [minor] `tests/openai-embedding-adapter.test.ts:234` — `RequestInfo` non résolu car `tsconfig.test.json` hérite `lib: ["ES2022"]` qui n'inclut pas DOM. Fix : soit remplacer `RequestInfo` par `RequestInit` (déjà global via `@types/node`), soit étendre `lib` dans `tsconfig.test.json` pour inclure `DOM`. La 1re option est plus minimaliste. (date: 2026-04-17, source: typecheck:tests)
- [x] [notable] `src/infra/run-manager.ts:107` — `updateManifest` performs non-atomic read-modify-write without a per-run serialization guard, so concurrent patches (e.g. `setInputFiles` racing with `stopRun`) can silently lose writes. Fix : serialize manifest mutations via a per-runDir async mutex (chained promise) or re-read inside a write loop with optimistic check-on-write. (date: 2026-04-17, source: senior-review nightly-clean iter-000)
- [x] [notable] `src/web/server.ts:108` — DELETE /api/runs/:id returns `{status: "stopped"}` with 200 even when the run finalized between `listRuns` and `rm.stopRun()` (`isTerminal` makes `stopRun` a no-op, so HTTP response misreports manifest state). Fix : re-read manifest via `rm.getManifest()` post-stop and return the actual status, or have `stopRun` return a boolean for transition success so the handler can respond 409 appropriately. (date: 2026-04-17, source: senior-review nightly-clean iter-000)
- [x] [notable] `src/infra/event-logger.ts:29` — `appendFile` is invoked from fire-and-forget `void eventLogger.emit(...)` calls in `pipeline.ts:77`, so concurrent in-flight writes to `events.jsonl` can interleave bytes (Node's `appendFile` doesn't serialize internally). Fix : serialize appends through a chained promise inside `createEventLogger` (queue each emit on a local writeChain); independently, consider awaiting emit() in pipeline.ts. (date: 2026-04-17, source: senior-review nightly-clean iter-000)
