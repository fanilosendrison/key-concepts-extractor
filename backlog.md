# Backlog

- [ ] [notable][blocked:prod-data] `src/infra/provider-shared.ts:10` — la valeur 600s n'est pas backée par des données empiriques (p95 latency). Instrumenter la latence des adapters LLM et ajuster data-driven. **Bloqué** : nécessite des runs LLM réels pour mesurer p95 ; `latencyMs` est déjà retourné par runWithRetry, aggregation/rollup à implémenter post-lancement (ne peut pas être fixé hors-prod). (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [nit] `src/infra/provider-shared.ts` — convention `name: "fnName: paramName"` des asserters (assertPositiveIntegerMs/assertNonNegativeFiniteMs) non documentée dans la signature. Si un 3e asserter arrive, risque de divergence de la convention. Fix : JSDoc `@param name "functionName: paramName"` OU refactor `(fnName, paramName, value)`. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [nit] `src/cli/commands/show.ts:40` — `line.slice(0, 80)` peut couper une surrogate pair UTF-16 en fin de preview, produisant un caractère de remplacement. Edge case improbable (JSON valide n'a pas de surrogates isolés, et la ligne est déjà malformed par hypothèse). Fix : append `.replace(/[\uD800-\uDBFF]$/, "")` après le scrub. (date: 2026-04-17, source: senior-review post-notable-fixes)

## Test typecheck debt — exposed by `tsconfig.test.json` / `pnpm typecheck:tests`

Contexte : le `tsconfig.json` projet excluait `tests/` du type-checking. L'ajout de `tsconfig.test.json` + script `typecheck:tests` a exposé 8 erreurs pré-existantes dans 5 fichiers de tests. Runtime OK (vitest 168/168), mais les invariants de types dérivent. À reboucher lot par lot ; une fois vide, câbler `typecheck:tests` dans la liste de vérifications obligatoires (CLAUDE.md projet) et en CI quand elle arrive.

- [ ] [notable] `tests/event-logger.test.ts:35,44,53` — `"tick"` passé comme PipelineEventType n'est pas dans `PIPELINE_EVENT_TYPES`. Soit le test simule un event synthétique qui devrait être rebaptisé en type publié, soit le test pré-date la fermeture de l'enum. Fix : renommer vers un type valide (ex. `extraction_progress`) ou caster explicitement avec justification. (date: 2026-04-17, source: typecheck:tests)
- [ ] [notable] `tests/fusion-intra.test.ts:6` — helper local accepte `string` pour un paramètre typé `ConceptCategory`. Erreur TS2322 masquée. Fix : typer le helper avec `ConceptCategory` et faire traverser les littéraux fermés. (date: 2026-04-17, source: typecheck:tests)
- [ ] [notable] `tests/run-manager.test.ts:50` — `RawConcept[]` avec objets partiels (manquent `granularity`, `justification`). Fix : compléter les fixtures ou utiliser la factory `rawConcept()` de `tests/helpers/factories.ts` qui remplit les defaults. (date: 2026-04-17, source: typecheck:tests)
- [ ] [notable] `tests/web-server.test.ts:88,91` — `rm.initRun(DEFAULT_RUN_CONFIG)` passe 1 argument ; la signature prod est `initRun(config, source)` depuis run-manager.ts:24. Signature drift masquée par l'absence de typecheck tests — le `source` est `undefined` à runtime, ce qui traverse silencieusement. Fix : passer un `RunSource` approprié (probablement `"cli"` ou `"web"` selon le scénario). (date: 2026-04-17, source: typecheck:tests)
- [ ] [minor] `tests/openai-embedding-adapter.test.ts:234` — `RequestInfo` non résolu car `tsconfig.test.json` hérite `lib: ["ES2022"]` qui n'inclut pas DOM. Fix : soit remplacer `RequestInfo` par `RequestInit` (déjà global via `@types/node`), soit étendre `lib` dans `tsconfig.test.json` pour inclure `DOM`. La 1re option est plus minimaliste. (date: 2026-04-17, source: typecheck:tests)
