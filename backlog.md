# Backlog

- [ ] [notable] `src/cli/commands/run.ts:22-25` + `src/pipeline.ts:252` — `formatEvent` produit `[time] run — run_complete {JSON}` au lieu du format humain prescrit par NIB-M-CLI §3.3 (`✅ Complete — N concepts — /path`). Drift préexistant, aggravé par la suppression de la ligne `Run X — status` (seul fallback humain lisible). Fix : spécialiser formatEvent pour `run_complete`/`run_error`/`run_stopped` selon §3.3. (date: 2026-04-17, source: senior-review post-options-refactor)
- [ ] [notable] `src/pipeline.ts:84-88` — `emitTerminal` catch silencieusement toute erreur I/O du logger. Si la persistence d'events.jsonl échoue exactement sur l'event terminal (disque plein, fd leak), le subscriber n'est jamais notifié, le CLI sort avec exit 1 et l'utilisateur ne voit aucun message de fin. Fix : écrire sur stderr dans le catch ou garantir la notification subscriber sync avant persistance. (date: 2026-04-17, source: senior-review post-options-refactor)
- [ ] [notable] `src/infra/provider-shared.ts:10` — la valeur 600s n'est pas backée par des données empiriques (p95 latency). Instrumenter la latence des adapters LLM et ajuster data-driven (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [notable] `src/infra/provider-shared.ts:44` — `composeSignal(external?, timeoutMs?)` ne valide pas `timeoutMs`. `AbortSignal.timeout(NaN|-1)` throw, `AbortSignal.timeout(0)` renvoie un signal déjà aborted. Aucun consommateur ne le provoque aujourd'hui mais l'API publique accepte n'importe quoi. Fix : mêmes gardes que `runWithRetry.maxTotalDurationMs` (`Number.isFinite && > 0`). (date: 2026-04-17, source: senior-review post-options-refactor)
- [ ] [notable] `src/infra/openai-embedding-adapter.ts:embed()` — pas de budget wallclock cumulatif sur l'opération multi-batch. Chaque batch borné à 120s, mais N batches non plafonnés (worst-case N × 120s). Use case nominal 100-200 concepts = 1-2 batches, non atteignable. Documenté par commentaire pour l'instant ; revoir si un use case dépasse 5 batches. Fix : ajouter `startedAt = Date.now()` global + check cumulatif entre batches. (date: 2026-04-17, source: senior-review post-batching-fix)
- [ ] [minor] `tests/openai-embedding-adapter.test.ts` — pas de test "batch N/M échoue fatal après que les précédents ont réussi". Sémantique actuelle = all-or-nothing (stack unwind perd les vecteurs partiels), raisonnable mais non verrouillée par test. Fix : ajouter T-EMB-09 qui fait échouer le 2e batch (mock bifurqué sur callIndex) et vérifie que `embed()` rejette sans retourner de vecteurs partiels. (date: 2026-04-17, source: senior-review post-batching-fix)
- [ ] [minor] `tests/provider-shared.test.ts:15-20` — T-PS-02 ne teste pas le nouveau paramètre `timeoutMs` de `composeSignal`. Ajouter un test qui passe un `timeoutMs` court (ex. 10ms), attend, et vérifie que le signal composite est aborted avec `name === "TimeoutError"`. (date: 2026-04-17, source: senior-review post-options-refactor)
- [ ] [minor] `src/infra/provider-shared.ts:81` — un timeout interne (`AbortSignal.timeout` fire) avec signal externe non aborted retombe en `TransientLLMError` et retry. Aligné avec NIB-M-PROVIDER-ADAPTERS mais aggrave le problème de budget global. À clarifier dans la spec ou documenter l'interaction (date: 2026-04-17, source: senior-review post-timeout-bump)
