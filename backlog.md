# Backlog

- [ ] [notable][blocked:prod-data] `src/infra/provider-shared.ts:10` — la valeur 600s n'est pas backée par des données empiriques (p95 latency). Instrumenter la latence des adapters LLM et ajuster data-driven. **Bloqué** : nécessite des runs LLM réels pour mesurer p95 ; `latencyMs` est déjà retourné par runWithRetry, aggregation/rollup à implémenter post-lancement (ne peut pas être fixé hors-prod). (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [minor] `src/infra/openai-adapter.ts` + `src/infra/google-adapter.ts` — validation `finish_reason` + extraction content dupliquée (truncation → TransientLLMError, safety/content_filter → FatalLLMError, puis extract content). Envisager un helper `extractLLMContent<T>(data, extractors)` dans `provider-shared.ts` avec extracteurs par provider. Non-trivial : les shapes varient (choices[0].finish_reason vs candidates[0].finishReason vs content[] blocks), la factorisation doit rester plus lisible que la duplication. (date: 2026-04-17, source: dedup-codebase post-batching-fix)
- [ ] [minor] `src/infra/provider-shared.ts:81` — un timeout interne (`AbortSignal.timeout` fire) avec signal externe non aborted retombe en `TransientLLMError` et retry. Aligné avec NIB-M-PROVIDER-ADAPTERS mais aggrave le problème de budget global. À clarifier dans la spec ou documenter l'interaction (date: 2026-04-17, source: senior-review post-timeout-bump)
- [ ] [nit] `src/infra/provider-shared.ts` — convention `name: "fnName: paramName"` des asserters (assertPositiveIntegerMs/assertNonNegativeFiniteMs) non documentée dans la signature. Si un 3e asserter arrive, risque de divergence de la convention. Fix : JSDoc `@param name "functionName: paramName"` OU refactor `(fnName, paramName, value)`. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [nit] `src/cli/commands/show.ts:40` — `line.slice(0, 80)` peut couper une surrogate pair UTF-16 en fin de preview, produisant un caractère de remplacement. Edge case improbable (JSON valide n'a pas de surrogates isolés, et la ligne est déjà malformed par hypothèse). Fix : append `.replace(/[\uD800-\uDBFF]$/, "")` après le scrub. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [minor] `src/infra/logger.ts:18` — `createLogger(dest?)` factory exportée sans caller in-tree, justifiée comme "future test seam". Si aucun test ne la consomme dans les semaines à venir, retirer (dead export) OU ajouter un test qui exerce l'injection. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [nit] `src/infra/logger.ts:18` — paramètre `dest` homonyme du key `dest` de `pinoDestination({ dest: 2 })`. Renommer en `destinationStream` pour lever l'ambiguïté au call-site. (date: 2026-04-17, source: senior-review post-notable-fixes)
- [ ] [notable][evaluated-deferred] `src/cli/format-event.ts:62-127` — 7 switch cases répètent le pattern `safeParse → warnAndFallback → format string`. **Évalué 2026-04-17** : refactor via `Record<EventType,{schema,format}>` perdrait l'exhaustivité TypeScript narrative (chaque case a son tuple de champs distinct utilisé inline dans le template string). Garder le switch ; gain LOC réel = 0, perte readability = nette. À reconsidérer si le nombre de cases dépasse ~12. (date: 2026-04-17, source: dedup-codebase post-senior-review-clean)
- [ ] [notable][evaluated-deferred] `src/domain/quality-controller.ts:165-231` ↔ `src/domain/relevance-controller.ts:116-202` — closures `aggregate` similaires en silhouette mais algorithmes génuinement distincts (Quality fait `buildCorrection`+`applyCorrections`+`splitCluster` avec inheritance ; Relevance accumule `removed`/`retained` puis filter par Set termsToRemove). **Évalué 2026-04-17** : `aggregateControlResults<Item,Output>` produirait gymnastique de génériques sans extraire de vrai code partagé. Reconsidérer si un 3e controller (e.g., novelty) émerge. (date: 2026-04-17, source: dedup-codebase post-senior-review-clean)
- [ ] [notable][evaluated-deferred] `src/domain/quality-controller.ts:36-42` ↔ `src/domain/relevance-controller.ts:40-44` — schémas Zod `R1ErrorSchema` / `R1FlagSchema`. **Évalué 2026-04-17** : seul champ partagé est `justification`, mais Quality utilise `z.string().optional()` (peut être absent) tandis que Relevance utilise `z.string().default("")` (toujours présent, défaut vide). Sémantiques divergentes — un `BaseControlItemSchema` forcerait à harmoniser une des deux conventions, ce qui changerait le wire contract observable. Reconsidérer si la convention diverge volontairement. (date: 2026-04-17, source: dedup-codebase post-senior-review-clean)
- [ ] [notable] `src/domain/control-shared.ts:37-60` ↔ `src/cli/format-event.ts:42-51` — deux parsers JSON+zod safeParse avec contrats divergents (throw ControlSchemaError vs return null). Signal d'anti-pattern émergent : un 3e parser déclencherait une factorisation `parseStrict<T>(schema, raw, {onFail: "throw"|"null"})`. Garder sous observation. (date: 2026-04-17, source: dedup-codebase post-senior-review-clean)
